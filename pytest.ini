[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    slow: real subprocess-heavy tests excluded from the default run (see evaluation harness); run explicitly with `-m slow` or by targeting the file directly.
addopts = -m "not slow"
