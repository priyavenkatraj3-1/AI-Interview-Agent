# Test-only dependencies, on top of requirements.txt.
-r requirements.txt
pytest==8.3.4
pytest-asyncio==0.25.2

# Evaluation-only: difficulty-vs-ability scatter plot (see evaluation/plot.py).
matplotlib==3.11.1
